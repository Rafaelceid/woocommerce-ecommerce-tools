# Ifiale e-commerce tools — Python dependencies
# Install: pip install -r requirements.txt
#
# Python >= 3.11 recommended

# Image processing
Pillow>=10.3.0

# Spreadsheet read/write
openpyxl>=3.1.2
xlrd>=2.0.1

# HTTP client (for csv normalizers, brand logo fetch)
requests>=2.31.0

# Data wrangling (csv_cloudinary_normalizer, process_taxonomy)
pandas>=2.2.0
numpy>=1.26.0

# Testing
pytest>=8.2.0
pytest-mock>=3.14.0

# Optional: PNG → SVG vectorisation
# vtracer>=0.6.3

# Optional: AVIF / HEIC image support
# pillow-avif-plugin
# pillow-heif
